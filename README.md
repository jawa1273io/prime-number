# prime-number

This repository contains a simple program for identifying prime numbers.

## Usage

The `prime_checker.py` script can be used from the command line:

```bash
python3 prime_checker.py NUMBER
```

This prints whether `NUMBER` is prime. To list all primes up to `NUMBER`, use the `--list` flag:

```bash
python3 prime_checker.py NUMBER --list
```
